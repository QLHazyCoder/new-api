# 敏感词与内容审计重构开发清单

## 阶段一：项目分析

- [x] 梳理 `controller/relay.go` 的协议统一入口、预扣费和上游调用顺序。
- [x] 梳理用户、令牌、分组定价、使用日志和管理员路由边界。
- [x] 对照 Sub2API 内容审计思路，确定“列表脱敏、详情保留证据、白名单只绕过拦截”的原则。

## 阶段二：后端数据与接口

- [x] 新增敏感词规则、规则分组、用户白名单、审计事件模型并纳入两套数据库迁移。
- [x] 全局规则与分组定价规则校验，禁止录入不存在的分组。
- [x] 新增配置、规则、分组、白名单、统计、审计、清理违规记录和解封接口。
- [x] 审计详情仅由管理员接口返回完整规范化提示词；列表接口清空 `full_prompt`。
- [x] 审计摘要遮蔽常见 API 令牌、Bearer 凭证和 URL，并保留提示词哈希与请求上下文。
- [x] 按配置的保留天数由系统任务定期清理完整提示词和摘要，不删除计数证据。

## 阶段三：请求处理与风控

- [x] 在预扣费、计费、上游请求之前执行敏感词检查。
- [x] 全局规则和当前实际分组局部规则统一匹配，旧 `setting.SensitiveWords` 保留过渡兼容。
- [x] 同一请求只计数一次；白名单用户记录审计和关键词拦截日志但不拦截、不增加次数。
- [x] 非白名单用户达到第五次命中时，在用户行锁事务中封禁、保留余额并刷新认证版本/缓存。
- [x] 使用日志新增类型 8“关键词拦截”，通过 `request_id` 和 `audit_id` 关联审计记录。
- [x] 使用明确的 `SensitiveWordsDetected` 错误和固定中文客户端提示。

## 阶段四：管理页面

- [x] 重构敏感词页面：运行状态、规则管理、用户白名单、最近审计记录四个区域。
- [x] 支持逐行批量录入敏感词、全局/局部范围、分组定价多选和客户端提示配置。
- [x] 统计卡片展示规则、今日命中、今日拦截、白名单放行和自动封禁。
- [x] 审计详情使用页面内对话框展示请求摘要、脱敏摘要和完整提示词。

## 阶段五：自检与验收

- [x] `gofmt`、`git diff --check`、`go build ./...`。
- [x] `npm run build`。
- [x] 敏感词规范化和提示词截断单元测试通过。
- [x] 检查中英文混用、乱码、列表泄露完整提示词和白名单计数语义。
- [x] 全量 `go test ./model ./service` 已执行；当前仓库的 SQLite 迁移测试在并发创建既有表时失败（`sensitive_word_rules`/`users` already exists），属于测试夹具/并发隔离问题，`service` 测试通过。

## 交付状态

实现已完成并通过构建级验收。保留旧敏感词配置作为过渡兼容，后续可在确认线上规则迁移完成后单独移除。
