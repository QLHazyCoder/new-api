# 敏感词与内容审计重构开发清单

本清单与 sensitive-word-content-audit-redesign.md 同步维护。状态只以当前工作树和实际验证结果为准。

## 0. 项目整体分析

- [x] 梳理 Relay 的请求解析、分组选择、预扣费、渠道重试和上游调用顺序。
- [x] 确认实际定价分组来源为 ratio_setting.GetGroupRatioCopy()，并确认 auto 需要检查全部候选组。
- [x] 梳理现有 SensitiveWords Option、Aho-Corasick 匹配、用户字段、使用日志和主/日志数据库边界。
- [x] 对照内容审计的列表脱敏、详情保留证据、结构化动作、运行时快照原则，排除外部审核供应商链路。

## 1. 数据模型与迁移

- [x] 在 User 增加 sensitive_word_violation_count 与 sensitive_word_whitelist。
- [x] 新增规则、规则词条、规则分组和审计事件模型。
- [x] 新词条采用子表；规则列表只统计词条数，不返回或预加载完整词条。
- [x] 正常和快速迁移路径均纳入新表和用户字段。
- [x] 从持久化旧 SensitiveWords Option 导入规则，并用迁移标记阻止规则删除后错误回退。
- [x] 异常旧词条不会阻塞启动或静默丢失，保留旧 Option 兼容匹配并记录系统日志。
- [x] 旧白名单表只作兼容元数据，运行时以用户字段为准。

阶段自检：

- [x] 旧 word 列保持原有带索引的 VARCHAR(200)，避免 MySQL 迁移到 TEXT 的索引兼容风险。
- [x] 审计事件始终写主库，使用日志仍可写独立 ClickHouse 日志库。
- [x] 用户违规字段默认值、JSON 字段和管理编辑映射一致。

## 2. 规则匹配与请求拦截

- [x] 建立全局/局部规则 Aho-Corasick 运行时快照。
- [x] 规则新增、编辑、启停、删除和旧 Option 更新后本进程立即刷新，无需重启。
- [x] 局部规则只允许分组定价中的真实分组，拒绝 auto。
- [x] 自动分组在预扣费前检查全部候选分组，局部命中写实际命中组。
- [x] 同一请求多规则、多词条命中只计数一次。
- [x] Relay 在 token 估算、预扣费、选渠道、上游调用和重试之前执行检查。
- [x] 审计事务失败时回滚计数，返回不可重试 503，不向用户伪报已记录。

阶段自检：

- [x] 全局规则覆盖所有分组。
- [x] 局部规则未绑定分组不命中。
- [x] 规则删除后已完成迁移的实例不回退旧 Option。
- [x] 提示词规范化、哈希、脱敏摘要和截断使用同一快照。

## 3. 白名单、违规次数与封禁

- [x] 白名单命中写审计和类型 8 日志，但不拦截、不计数。
- [x] 观察模式命中只记录，不拦截、不计数、不封禁。
- [x] 普通用户命中在用户行锁事务中原子增加违规次数。
- [x] 第 5 次有效命中只封禁一次，更新认证版本并撤销会话。
- [x] 管理员可编辑或清零次数，白名单开关在同一用户编辑抽屉维护。
- [x] 所有封禁和用户编辑路径均不修改 quota 或余额。

阶段自检：

- [x] 第 1、4、5、6 次行为有自动化测试。
- [x] 第 5 次后会话撤销，之后不重复封禁。
- [x] 审计事件的前后 quota 快照相同，日志明确写入 balance_changed:false。
- [x] 管理员清零违规次数不删除历史证据。

## 4. 审计与使用日志

- [x] 新增日志类型 8：关键词拦截。
- [x] 日志通过 request_id 与 audit_id 关联主库审计事件。
- [x] 管理员详情保留完整规范化提示词、命中规则/词条/片段、分组、协议、状态变化和处理结果。
- [x] 日志列表不返回完整提示词。
- [x] 普通用户日志自动去除 audit_id、词条、规则名称和提示词哈希。
- [x] 审计证据可配置保存；到期清理只清空提示词和摘要，保留元数据。

阶段自检：

- [x] 控制器回归测试证明审计列表不泄露完整提示词，详情保留管理员复核证据。
- [x] 错误响应是 HTTP 403、sensitive_words_detected、不可重试且不重复写普通错误日志。
- [x] OpenAI Chat、Responses、Claude、Gemini、图片请求的提示词提取已有独立模块测试。

## 5. 管理界面

- [x] 敏感词页只保留策略设置、统一规则表、规则弹窗和日志入口。
- [x] 全局与局部规则放在同一表格，右侧操作列支持编辑、启用/停用和永久删除。
- [x] 规则弹窗支持文本粘贴、TXT 上传、范围选择、定价分组多选和解析预览。
- [x] 用户列表展示违规次数/白名单状态。
- [x] 用户编辑右抽屉在分组/备注之后、权限之前新增内容安全区。
- [x] 使用日志支持类型 8 筛选和管理员详情抽屉。

阶段自检：

- [x] 敏感词页不加载审计列表、违规统计或白名单名单。
- [x] 规则表不显示完整词条。
- [x] 普通用户没有审计详情加载路径。
- [x] 所有新增中文文本使用 UTF-8，无 replacement character。

## 6. 文档与维护

- [x] 新增架构、接口、迁移、回滚和排障文档。
- [x] 更新维护指南中的敏感词操作和发布边界。
- [x] 记录文件职责、测试入口、旧配置兼容和禁止余额清零的硬约束。
- [ ] 最终提交后回填提交号、GitHub Actions 和部署结果。

## 7. 最终验证门禁

- [x] gofmt 与 git diff --check。
- [x] 模型针对性测试：规则、旧配置迁移、白名单、观察模式、第五次封禁、余额不变、运行时刷新和用户日志脱敏。
- [x] 控制器针对性测试：用户抽屉字段持久化、审计列表脱敏、Relay 403 早期拦截、端点查询参数脱敏和不产生消费日志。
- [x] relaykit 独立模块提示词提取和错误契约测试。
- [x] 根 Go 模块全量测试和构建：`go test ./... -count=1`、`go build ./...`。
- [x] 前端类型检查、生产构建和项目 lint：`npm run typecheck`、`npm run build:check`、`npm run lint`。lint 为退出码 0，输出仅含既有非本次文件警告。
- [x] 前端定向 Vitest：`npm test -- src/features/wallet/lib/payment.test.ts`，4 项通过。
- [ ] 前端全量 Vitest：当前基线仍有 8 个测试文件导入 `node:test`/`bun:test` 导致 Vite 解析失败，另有 2 个既有 API Key 抽屉用例超时；本次敏感词相关文件未在失败清单中。发布前需由维护者决定是否单独修复该存量测试债务。
- [ ] 前端全量格式检查：`npm run format:check` 当前仅报告 5 个既有无关文件（`response-fade-*`、`response-renderer-inline.tsx`、`api-key-group-cell.tsx`、`redemption-form.ts`）；本次敏感词文件未在失败清单中。
- [ ] GitHub Actions 成功。
- [ ] 蓝绿发布、线上健康检查和观察窗口。

## 最终审查结论

代码实现、定向回归、根 Go 全量测试、relaykit 测试、前端类型检查、生产构建和项目 lint 已完成。前端全量 Vitest 和格式检查的已知存量失败未被掩盖；GitHub Actions、提交和授权的线上更新仍待完成。完成后必须在本节补充实际提交号、Actions run 和发布结果；不得以设计完成或局部测试通过替代最终交付结论。
